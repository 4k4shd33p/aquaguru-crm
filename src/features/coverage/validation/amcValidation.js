export function validateAmc(values) { const errors = {}; if (!values.equipmentId) errors.equipmentId = 'Select equipment.'; if (!values.startDate) errors.startDate = 'Start date is required.'; if (!values.endDate) errors.endDate = 'End date is required.'; if (values.startDate && values.endDate && values.endDate < values.startDate) errors.endDate = 'End date must be on or after the start date.'; for (const key of ['standardPrice', 'agreedPrice', 'plannedVisits']) if (values[key] !== '' && values[key] !== null && Number(values[key]) < 0) errors[key] = key === 'plannedVisits' ? 'Planned visits cannot be negative.' : 'Prices cannot be negative.'; return errors }

