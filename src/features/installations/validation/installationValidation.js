export function validateInstallation(values) { const errors = {}; if (!values.equipmentId) errors.equipmentId = 'Select equipment before continuing.'; if (!values.classification) errors.classification = 'Select an installation classification.'; return errors }
export function validateCompletion(values) { const errors = {}; if (!values.installationDate) errors.installationDate = 'Enter the actual installation date.'; for (const key of ['tdsIn', 'tdsOut', 'installationCharge', 'technicianCharge']) if (values[key] !== '' && values[key] !== null && Number(values[key]) < 0) errors[key] = 'TDS and charges cannot be negative.'; return errors }

